# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local dev copy of root data/ — CI always regenerates this fresh at deploy
# time (see .github/workflows/deploy-pages.yml), so it's never a source of
# truth and shouldn't be committed.
public/data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
